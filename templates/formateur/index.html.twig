{% extends 'base.html.twig' %}

{% block title %}Listes des Formateurs{% endblock %}

{% block body %}

    <h1>Liste des Formateurs</h1>

    <section class="section_listesFormateursStagiaires">
        <table>
            <thead>
                <tr>
                    <th>Nom Prenom</th>
                    <th>Email</th>
                    <th>Sessions gérées</th>
                </tr>
            </thead>
            <tbody>
                {% for formateur in formateurs %}
                <tr>
                    <a href="{{ path('app_ficheFormateur', {'id' : formateur.id} ) }}" class="container_formateur_stagiaire">
                        <td>{{ formateur.nom }} {{formateur.prenom}}</td>
                        <td>{{ formateur.email }}</td>
                        <td class="nbr_session">{{ formateur.sessions | length }}</td>
                    </a>
                </tr>
                {% endfor %}
            </tbody>
        </table>

    </section>
{% endblock %}
